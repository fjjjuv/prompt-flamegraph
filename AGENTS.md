# Project notes for agents

- Test: `PYTHONPATH=src python3 -m pytest tests/ -v` (always set PYTHONPATH=src).
- CLI: `PYTHONPATH=src python3 -m prompt_flamegraph ...`.
- Python >=3.10, zero required dependencies. Optional: tiktoken, rich.
- Commits: authored by Fjjjuv <julienjuvany@gmail.com> — NEVER add
  "Generated with ..." or "Co-Authored-By" trailers; the repo owner is
  the only contributor.
- Push only when explicitly asked.
